# ── Web framework ──────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9
httpx==0.27.0

# ── Database ───────────────────────────────────────────────────
sqlalchemy==2.0.30
alembic==1.13.1
psycopg2-binary==2.9.9
asyncpg==0.29.0         # async postgres driver

# ── Task queue ────────────────────────────────────────────────
celery==5.4.0
redis==5.0.4
celery[redis]==5.4.0

# ── NLP — CPU-optimised builds ────────────────────────────────
# Install torch CPU separately first:
# pip install torch==2.3.0 --index-url https://download.pytorch.org/whl/cpu
transformers==4.41.0
tokenizers==0.19.1
sentence-transformers==3.0.0   # SBERT for clustering
spacy==3.7.4
# After install: python -m spacy download en_core_web_sm
onnxruntime==1.18.0            # quantized model inference (fast CPU)
optimum==1.21.4               # HuggingFace → ONNX export helper

# ── Clustering ────────────────────────────────────────────────
hdbscan==0.8.33
umap-learn==0.5.6
scikit-learn==1.5.0
numpy==1.26.4
scipy==1.13.1

# ── Signals & prediction ──────────────────────────────────────
lightgbm==4.3.0
pandas==2.2.2
statsmodels==0.14.2

# ── Data ingestion ────────────────────────────────────────────
feedparser==6.0.11        # RSS parsing
httpx==0.27.0             # async HTTP
beautifulsoup4==4.12.3
lxml==5.2.2
gdeltdoc==1.5.0           # GDELT Python client
yfinance==0.2.40          # Yahoo Finance

# ── Utilities ─────────────────────────────────────────────────
pydantic==2.7.1
pydantic-settings==2.2.1
python-dotenv==1.0.1
tenacity==8.3.0           # retries with backoff
structlog==24.1.0         # structured logging
prometheus-fastapi-instrumentator==7.0.0
apscheduler==3.10.4       # lightweight scheduler (alternative to Celery beat for small deploys)

# ── Testing ───────────────────────────────────────────────────
pytest==8.2.0
pytest-asyncio==0.23.7
pytest-cov==5.0.0
httpx==0.27.0             # async test client
